version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: regz-postgres
    restart: always
    environment:
      POSTGRES_USER: regz_user
      POSTGRES_PASSWORD: regz_password
      POSTGRES_DB: regz_db
    ports:
      - "${POSTGRES_PORT:-5434}:5432"
    volumes:
      - regz_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U regz_user -d regz_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: regz-app
    restart: always
    environment:
      PORT: 4000
      NODE_ENV: production
      DATABASE_URL: postgres://regz_user:regz_password@postgres:5432/regz_db
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: regz_user
      DB_PASSWORD: regz_password
      DB_NAME: regz_db
    depends_on:
      postgres:
        condition: service_healthy

  duckdns:
    image: lscr.io/linuxserver/duckdns:latest
    container_name: regz-duckdns
    restart: always
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Sao_Paulo
      - SUBDOMAINS=${DUCKDNS_SUBDOMAINS:-regiz-app}
      - TOKEN=${DUCKDNS_TOKEN}
      - LOG_FILE=true

  caddy:
    image: caddy:2-alpine
    container_name: regz-caddy
    restart: always
    ports:
      - "80:80"
      - "8280:8280"
    command: caddy reverse-proxy --from https://${DUCKDNS_SUBDOMAINS:-regiz-app}.duckdns.org:8280 --to frontend:4000
    volumes:
      - regz_caddy_data:/data
      - regz_caddy_config:/config
    depends_on:
      - frontend

volumes:
  regz_postgres_data:
    driver: local
  regz_caddy_data:
    driver: local
  regz_caddy_config:
    driver: local
